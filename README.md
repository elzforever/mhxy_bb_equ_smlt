# 梦幻高级工具箱 - 极速部署指南

本项目采用模块化架构，为了简化本地部署流程，我们提供了一个自动化脚本。

## 🚀 30秒极速部署

1. **初始化项目** (如果您还没创建):
   ```bash
   npm create vite@latest mhxy-toolbox -- --template react-ts
   cd mhxy-toolbox
   npm install
   ```

2. **创建安装脚本**:
   在项目根目录（`mhxy-toolbox/`）下创建一个名为 `install.js` 的文件，将提供的脚本代码完整粘贴进去。

3. **运行自动部署**:
   ```bash
   node install.js
   ```
   *该脚本会自动创建 `src/features` 等所有文件夹，写入所有源代码，并配置好 TailwindCSS。*

4. **启动**:
   ```bash
   npm run dev
   ```

---

## 功能列表

*   **召唤兽装备计算器** (`features/SummonedBeastEquipCalculator.tsx`): 包含历史记录、性价比分析、全属性录入。
*   **属性模拟器** (`features/SummonedBeastSim.tsx`)
*   **灵饰分析** (`features/SpiritAccessoryCalculator.tsx`)
*   **宝石计算** (`features/GemPriceCalculator.tsx`)
